# GEMINI_API_KEY: (Bắt buộc nếu dùng AI lyrics) Google Gemini API key.
# Tạo tại https://aistudio.google.com/apikey
GEMINI_API_KEY=""

# VITE_WORKER_URL: (Không bắt buộc) URL Cloudflare Worker proxy.
# Deploy worker/index.js lên Cloudflare Workers, dán URL ở đây.
# Nếu không set, web vẫn chạy nhưng file lớn (>4.5MB) sẽ bị Vercel chặn.
VITE_WORKER_URL=""
<<<<<<< HEAD

# VITE_GOOGLE_API_KEY: (Không bắt buộc) Google API key cho Drive API.
# Tạo tại https://console.cloud.google.com/apis/credentials -> Create Credentials -> API Key
# Enable "Google Drive API" trong Library. Dùng chung 1 key cho tất cả user.
# Nếu không set, Drive URL sẽ fallback dùng direct download (kém ổn định).
VITE_GOOGLE_API_KEY=""
=======
>>>>>>> parent of 7f098e5 (u)
